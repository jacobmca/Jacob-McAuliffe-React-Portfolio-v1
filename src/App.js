import React from 'react';
import { BrowserRouter as Router, Route, Routes } from 'react-router-dom';
import { Header, Navigation, Project, Footer } from './components';
import { AboutMe, Contact, Portfolio, Resume } from './components/pages';

function App() {
  
}

export default App;
